export enum orderTableEnum {
  Order = 'Order',
}

export enum orderTableEnumMSG {
  timeMustBe = 'time must be later than the current time.',
  Min = 'must be greater than or equal to',
}

export enum orderTableEnumConfig {
  MinTotalAmount = 1,
  MaxTotalAmount = 99999
}